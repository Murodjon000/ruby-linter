# rubocop:disable all
class Board
  def initialize(player, board)
    @player = player] 
     @board = board
  end

  def board(lines)
    lines.each do |x|
      puts x
    end
  
end
# rubocop:enable all